@{
    if (!WebSecurity.IsAuthenticated)
    {
        Response.Redirect("~/Account/Login?returnUrl="
            + Request.Url.LocalPath);
    }
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    Page.Title = "Members Information";
}

<b>Pagina solo para miembros</b>